# Restore Usage Tracker to port 8787

The user corrected the earlier port request: 8447 belongs to the wrong project.
Usage Tracker must run on 8787. This supersedes the port-8447 plan and its
permission to terminate arbitrary listeners.

## Implementation

1. Set the project's deployment port back to 8787 throughout runtime defaults,
   CLI validation, tests, and current documentation.
2. Remove the arbitrary listener-termination routine. Redeployment stops only
   the recognized Usage Tracker LaunchAgent, then verifies 8787 can be bound.
   If another process occupies 8787, report the conflict instead of killing it
   or selecting another port.
3. Correct root AGENTS.md and the saved future-reference note; annotate the
   superseded plan so 8447 cannot be mistaken for the current instruction.
4. Run the tests, redeploy the existing service with the same data directory,
   verify health on 8787 and shutdown of our temporary 8447 service, then move
   the dashboard tab to 8787.

## Results

- On September 9, the recognized Usage Tracker LaunchAgent was redeployed with
  `python3 -m usage_tracker service install`.
- The service now listens only on `127.0.0.1:8787`; `/api/health` returned `ok`
  with zero collection errors.
- Port 8447 was released for Localhost Index. No unrelated listener was stopped.

### Final validation

- Runtime defaults, CLI/service validation, README, dashboard documentation, and
  AGENTS.md use 8787. The arbitrary listener discovery and TERM/KILL routine was
  removed. A new memory note supersedes the mistaken 8447 instruction.
- Full suite: 234 tests passed. The 10 focused service tests passed again after
  moving one assertion out of an unreachable exception block.
- Final redeployment retained the existing usage data directory and 30-second
  collection interval. The saved LaunchAgent uses `--port 8787`; HTTP health
  returned 200, with 456 files scanned and zero errors.
- The unrelated Node listener on 8447 remained untouched. The dashboard tab
  was moved to 8787 and the daily usage page loaded successfully.
