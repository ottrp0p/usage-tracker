# Deploy Usage Tracker only on port 8447

**WITHDRAWN by the user on September 9, 2026:** This instruction was for the
wrong project. Usage Tracker must return to 8787; do not follow this plan's
listener-termination instruction. See [the correction](2026-09-09-restore-port-8787.md).

The user explicitly requires this project to always deploy on port 8447 and to
terminate any process listening there before redeployment. This supersedes the
original 8787 proposal and the earlier instruction to choose another free port.

## Implementation

1. Centralize port 8447 as the runtime default; reject other deployment ports
   in the CLI and service installer. Internal test servers may use ephemeral
   ports so tests never interfere with the running service.
2. On each service installation/redeployment, unload the previous project
   LaunchAgent, terminate TCP listeners on 8447, verify the port is free, and
   reinstall the project on 8447. Use bounded termination and explicit failure;
   never fall back to another port or modify the usage database.
3. Update the README, current dashboard documentation, and root AGENTS.md with
   the standing deployment rule. Preserve historical plan text with a clear
   superseding notice. Record the user's explicit future preference in memory.
4. Test service lifecycle ordering and failure behavior with mocked processes,
   redeploy the actual service, verify health on 8447 and release of 8787, then
   move the existing dashboard tab to 8447.

## Results

Before the user corrected the request, 247 tests passed and the service was
briefly deployed on 8447 with health reporting zero collection errors. The
correction plan records restoration to 8787.
